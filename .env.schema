# @defaultSensitive=true

# Replicate API token — used for upscaling furniture-in-interior gallery images
# https://replicate.com/account/api-tokens
# @type=string(startsWith=r8_) @required @sensitive
REPLICATE_API_TOKEN=
